import {
  __commonJS
} from "./chunk-ROME4SDB.js";

// node_modules/fisher-yates/index.js
var require_fisher_yates = __commonJS({
  "node_modules/fisher-yates/index.js"(exports, module) {
    module.exports = function shuffle(array, rng) {
      rng = rng || Math.random;
      const result = [];
      for (let i = 0; i < array.length; ++i) {
        const j = Math.floor(rng() * (i + 1));
        if (j !== i) {
          result[i] = result[j];
        }
        result[j] = array[i];
      }
      return result;
    };
  }
});
export default require_fisher_yates();
//# sourceMappingURL=fisher-yates.js.map
